export interface Categoria {
  idCategoria: number;   // corresponde a id_categoria en la DB
  nombre: string;        // corresponde a nombre en la DB
  activo: boolean;       // corresponde a activo en la DB
}